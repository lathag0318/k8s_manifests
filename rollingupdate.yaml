apiVersion: apps/v1 
kind: Deployment
metadata:
  name: rollingupdate
  namespace: k8s-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: alpineapp
  strategy: 
    type: RollingUpdate
  template:
    metadata:
      name: alpinepod
      labels:
        app: alpineapp
    spec:
      containers:
        - name: alpinecontainer
          image: dokerhandson/maven-web-app:1
          ports:
            - containerPort: 8090

---
apiVersion: v1
kind: Service
metadata:
  name: alpinesvc
  namespace: k8s-ns
spec:
  type: NodePort
  selector:
    app: alpineapp
  ports:
   -  port: 80
      targetPort: 8090
